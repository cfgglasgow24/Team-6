import React from 'react';

const HeroSection: React.FC = () => {
    return (
        <section>
            {
                <h1>Hero Section</h1>
            }
        </section>
    );
};

export default HeroSection;